<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uco.avalon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EquipmentOptionsActivity"></activity>

        <activity
            android:name=".ProjectSelection"
            android:label="@string/title_activity_project_selection"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProjectOverviewActivity"></activity>
        <activity android:name=".ActivityHome" />
        <activity android:name=".ActivityResetPassword" />

    </application>

</manifest>